# Code Style Rules

## Function
### use Named Export and Arrow Function

## Naming Conventions
### Folder — `kebab-case`
### File (Component) = `PascalCase`
### File (Not a Component) = `camelCase`
### Hook File — `kebab-case` with `use-` prefix
### Hook Function - `camelCase` with `use` prefix
